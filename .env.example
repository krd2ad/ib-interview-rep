# No server-side environment variables needed.
# The Anthropic API key is entered by the user in the browser and stored in localStorage.
#
# For local development with npm run dev, you can optionally set:
# VITE_BASE_PATH=/     (default, use for local dev and custom domains)
#
# GitHub Actions automatically sets VITE_BASE_PATH to /repo-name/ during deployment.
